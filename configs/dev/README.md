# configs/dev — empty by design.
# T4 / smoke / micro fixture YAMLs were removed from the MAGI program path.
# Unit fixtures live inline in tests/ (temp YAML), not as committed product configs.
# Production / bring-up configs live under configs/*.yaml
